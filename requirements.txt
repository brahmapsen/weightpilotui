# Core Streamlit dependencies
streamlit>=1.23.0
streamlit-agraph

# Voice recording dependencies (using built-in st.audio_input)
SpeechRecognition
# pyaudio

# API and HTTP requests
requests
python-dotenv

# Audio processing
pydub
